import React, { Component } from "react";
import styled from "styled-components";

const Inner = (props) => {
    return (
        <div>
            <div> This is Inner page </div>
        </div>
    );
}

export default Inner;

